import 'package:flutter/material.dart';

class WatchlistTab extends StatelessWidget {
  const WatchlistTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text("Watchlist Tab", style: TextStyle(color: Colors.white)));
  }
}
